[package]
name = "rastro"
description = "Emits a canonical, diffable fingerprint of a Linux host's state"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "../../README.md"
keywords = ["fingerprint", "drift", "state", "sysadmin", "devops"]
categories = ["command-line-utilities"]
publish = false

[dependencies]
rastro-collector = { workspace = true }
rastro-fingerprint = { workspace = true }
clap = { workspace = true }
serde = { workspace = true }
# Shelling out to a canonical tool, hardened: `which` resolves an absolute path
# before exec, `subprocess` caps both the time and the output of a run.
subprocess = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
which = { workspace = true }

[dev-dependencies]
rastro-fingerprint = { workspace = true }
# Tests read the rendered document back to assert on its shape.
serde_json = { workspace = true }
