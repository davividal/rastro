# Rust
/target

# macOS
.DS_Store

review-results.md

# Fingerprints are sensitive operational data, see SECURITY.md.
/rastro-*.json
/reports/

# Transient agentic-workflow artifact.
/.code-goal-run.md

# Where a coverage run's noise lands when somebody redirects it. The report itself goes to
# `target/llvm-cov/`, which `/target` already covers; this is the stderr beside it, and one
# copy reached a commit before this line existed.
/cov.err
